{% extends "layout.html.twig" %}

{% block content %}
    <div class="row">
        <div class="offset-lg-3 col-lg-6 offset-md-2 col-md-8 offset-sm-1 col-sm-10 mt-3">
            <div class="card">
                <div class="card-header">
                    Résultat de la rencontre {{ game.homeTeam.name }} <img  class="ms-1 me-3" src="{{ asset(game.homeTeam | flag(32) ) }}" />
                         - <img class="ms-3 me-1" src="{{ asset(game.awayTeam | flag(32) ) }}" />{{ game.awayTeam.name }}
                </div>
                <div class="card-body">
                    {{ form_start(form) }}
                    {{ form_row(form.goalsHome) }}
                    {{ form_row(form.goalsAway) }}
                    {{ form_row(form.extraTime) }}
                    {{ form_row(form.penaltiesHome) }}
                    {{ form_row(form.penaltiesAway) }}
                        <div class="btn-group">
                            {{ form_row(form.submit, {'attr' : {'class' : 'btn-success mr-3'} }) }}
                            {{ form_row(form.cancel) }}
                        </div>
                    {{ form_end(form) }}
                </div>
            </div>
        </div>
    </div>
    
{% endblock content %}

